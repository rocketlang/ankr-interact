# ANKR Interact — Environment Configuration
# Copy to .env and fill in your values

# ── Required ──────────────────────────────────────────────────────────────────
PORT=3199
DATABASE_URL=postgresql://interact:yourpassword@localhost:5432/interact
SECRET_KEY=change-me-to-a-random-string-at-least-32-chars-long

# ── AI Features (optional — app works without these) ──────────────────────────
# Use Ollama (local, free, private):
AI_PROXY_URL=http://localhost:11434
# OR use cloud AI (OpenAI / Anthropic key):
AI_API_KEY=

# ── Email (optional — for notifications) ──────────────────────────────────────
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@yourdomain.com

# ── Payments (optional — for marketplace/subscriptions) ──────────────────────
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ── Storage (optional — default: local filesystem) ────────────────────────────
# STORAGE_TYPE=local
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=

# ── Feature Flags ─────────────────────────────────────────────────────────────
# Override individual features (see src/config/features.ts)
# FEATURE_MARKETPLACE=false
# FEATURE_AI_TUTOR=false
# FEATURE_COLLAB=true
