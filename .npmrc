# ANKR Interact — npm configuration
# Apache 2.0 — open source, public access

# Scoped package: @ankr/interact
@ankr:registry=https://registry.npmjs.org

# Verdaccio (local dev / self-hosted registry)
# To publish to your own Verdaccio instance instead of npm:
#   @ankr:registry=http://localhost:4873
#   //localhost:4873/:_authToken=${VERDACCIO_TOKEN}

# npm publish auth (set via environment or `npm login`)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
